/**
 * components/ui/Skeleton.tsx
 *
 * Canonical re-export of the global Skeleton primitive so both import paths
 * resolve to the same implementation without duplicating shimmer logic:
 *
 *   import { Skeleton } from "@/components/Skeleton";      // root path
 *   import { Skeleton } from "@/components/ui/Skeleton";   // ui path (shadcn-style)
 */
export { Skeleton } from "@/components/Skeleton";
